{
  "name": "payrails-tokenization-demo-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild frontend/src/main.js --bundle --minify --sourcemap --outdir=static/dist",
    "build:dev": "esbuild frontend/src/main.js --bundle --sourcemap --outdir=static/dist"
  },
  "dependencies": {
    "@payrails/web-sdk": "5.39.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
