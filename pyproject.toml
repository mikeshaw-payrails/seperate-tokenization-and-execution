[tool.poetry]
name = "payrails-tokenization-demo"
version = "0.1.0"
description = "Minimal Payrails card element tokenization demo using Flask"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = []

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
flask = "^3.0.2"
requests = "^2.31.0"
python-dotenv = "^1.0.1"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=1.7.0"]
build-backend = "poetry.core.masonry.api"
